# Frontend

The `frontend/` directory is a Create React App project that drives the login screen.

## Commands

```bash
npm install
npm start
```

You can also run `npm test` or `npm run build` from inside this folder.
